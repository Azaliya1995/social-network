let initialState = {
    friends: [
        {id: 1, friendName: "Uzali"},
        {id: 2, friendName: "Urslany"},
        {id: 3, friendName: "Alan"}
    ]
};

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;